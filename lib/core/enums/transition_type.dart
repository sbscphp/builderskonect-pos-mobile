enum TransitionType {
  native, // Uses platform default
  fade, // Cross-fade
  slideRight, // Slide from right
  slideLeft, // Slide from left
  slideTop, // Slide from top
  slideBottom, // Slide from bottom
  scale, // Scale up
  rotate, // Rotate + scale
  size, // Size transition
  custom, // Fully custom transition
}
